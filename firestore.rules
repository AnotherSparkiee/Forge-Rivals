rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Полный доступ для прототипирования. 
    // Разрешает чтение и запись для всех коллекций без ограничений.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
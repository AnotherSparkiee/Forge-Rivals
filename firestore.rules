rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // АБСОЛЮТНО ОТКРЫТЫЕ ПРАВИЛА ДЛЯ ПРОТОТИПА
    match /{document=**} {
      allow read, write, delete: if true;
    }
  }
}
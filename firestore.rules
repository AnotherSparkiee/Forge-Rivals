rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Глобальный доступ для всех коллекций для устранения ошибок Access Denied в прототипе
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
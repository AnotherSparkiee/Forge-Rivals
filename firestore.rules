rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ПОЛНЫЙ ДОСТУП ДЛЯ ПРОТОТИПА
    // Позволяет всем пользователям читать и записывать любые данные во всех коллекциях.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Радикально открытые правила для прототипа.
    // Разрешаем абсолютно любые операции чтения и записи всем пользователям.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Полный доступ для прототипирования - устраняет любые ошибки Access Denied
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Полный доступ ко всем коллекциям для тестирования и бесперебойной работы всех систем
    match /{document=**} {
      allow read, write, delete: if true;
    }
  }
}
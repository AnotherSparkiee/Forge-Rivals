rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Абсолютный доступ ко всем документам для устранения ошибок в Studio
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

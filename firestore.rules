rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Radically open rules for prototyping.
    // Allows all read/write operations for all collections.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

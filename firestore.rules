rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Абсолютный доступ ко всем коллекциям для прототипирования
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
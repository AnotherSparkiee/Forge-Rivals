rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Глобальное разрешение для всех коллекций на время разработки и отладки
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
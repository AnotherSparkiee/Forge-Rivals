{
  "entities": {
    "PlayerProfile": {
      "collection": "players_v10",
      "properties": {
        "selectedLeagueId": "string (ALPHA, BETA...)",
        "leagueLevel": "number (1-9)",
        "groupId": "number (1-256)",
        "tournaments": "array (active tour IDs)",
        "setupDate": "ISO date"
      }
    },
    "LeagueGroup": {
      "collection": "leagues_v2/{leagueId}/divisions/{divId}/groups/{groupId}",
      "description": "State for a group of 8 teams",
      "properties": {
        "seasonId": "number",
        "teams": "array (8 unique IDs)",
        "lastProcessedDate": "YYYY-MM-DD"
      }
    },
    "Match": {
      "collection": "matches_v1",
      "idFormat": "m_{league}_{div}_{group}_s{season}_d{day}_h{homeId}",
      "properties": {
        "status": "pending | finished",
        "startTime": "ISO date",
        "seasonNumber": "number",
        "scoreA": "number",
        "scoreB": "number"
      }
    }
  },
  "automation": {
    "season_cycle": "16 days (14 play + 2 break)",
    "epoch": "2026-06-17T00:00:00+03:00",
    "algorithm": "Round-robin (deterministic)",
    "conflict_resolution": "Aggressive purge of legacy bot names on season start"
  }
}
